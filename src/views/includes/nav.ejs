<nav class="navbar bg-dark border-bottom border-bottom-dark" data-bs-theme="dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Agenda</a>

      <% if(!user) {%>
        <a class="btn btn-outline-success" type="submit" href="/login">Entrar / Criar</a>
      <% }else{ %>
        <div class="d-flex">
        <a class="btn btn-outline-info me-3" type="submit" href="/contato">Cadastrar contato</a>
        <a class="btn btn-outline-danger" type="submit" href="/login/logout">Sair</a>
      </div>
       <% } %>
    </div>
  </nav>
